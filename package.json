{  
  "scripts": {
    "pack": "build --dir",
    "dist": "build",
    "postinstall": "install-app-deps"
  },
  "build": {
    "appId": "org.coolhanddev.builder",
    "mac": {
      "category": "public.app-category.utilities"
    },
    "win": {
      "iconUrl": "./build/background.tiff"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ]
    }
  },
  "devDependencies": {
    "electron": "^1.4.8",
    "electron-builder": "^8.6.0"
  } 
}